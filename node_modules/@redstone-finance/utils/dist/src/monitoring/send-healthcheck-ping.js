"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.sendHealthcheckPing = void 0;
const axios_1 = __importDefault(require("axios"));
const logger_1 = require("../logger");
const logger = (0, logger_1.loggerFactory)("healthcheck");
const sendHealthcheckPing = async (healthcheckPingUrl) => {
    if (!healthcheckPingUrl) {
        return;
    }
    const start = Date.now();
    try {
        await axios_1.default.get(healthcheckPingUrl);
        logger.info(`Sent healthcheck ping in ${Date.now() - start}ms`);
    }
    catch (e) {
        const error = e;
        if (error.code === "ENOTFOUND") {
            logger.error("Healthcheck address not found. Configure your `HEALTHCHECK_PING_URL` properly");
        }
        else {
            logger.error("Unknown error occurred, when trying to health check ping", error);
        }
    }
};
exports.sendHealthcheckPing = sendHealthcheckPing;
//# sourceMappingURL=send-healthcheck-ping.js.map