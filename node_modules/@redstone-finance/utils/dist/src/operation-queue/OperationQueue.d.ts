export type Operation = () => Promise<void>;
export declare class OperationQueue {
    private logger;
    private queue;
    private isProcessing;
    private activeOperations;
    constructor(logger?: import("../logger").RedstoneLogger);
    enqueue(id: string, operation: Operation): boolean;
    private processQueue;
}
//# sourceMappingURL=OperationQueue.d.ts.map