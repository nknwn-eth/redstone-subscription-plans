import { FnBox } from "./FnBox";
export declare abstract class Executor<R> {
    protected readonly logger: import("../logger").RedstoneLogger;
    static getPromises<R>(functions: FnBox<R>[], timeoutMs?: number): Promise<R>[];
    static execFn<R>(func: FnBox<R>, timeoutMs?: number): Promise<R>;
    private static makeLogData;
    abstract execute(functions: FnBox<R>[]): Promise<R>;
}
//# sourceMappingURL=Executor.d.ts.map