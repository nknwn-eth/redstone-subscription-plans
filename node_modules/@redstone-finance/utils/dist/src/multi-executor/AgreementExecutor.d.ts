import { ParallelExecutor } from "./ParallelExecutor";
export declare class AgreementExecutor<R> extends ParallelExecutor<R> {
    private readonly quorumNumber;
    protected shouldResolveUnagreedToUndefined: boolean;
    constructor(quorumNumber: number, timeoutMs: number | undefined, shouldResolveUnagreedToUndefined?: boolean);
    aggregate(results: R[]): R;
    verifySettlements(successfulResults: R[], errorResults: unknown[], totalLength: number): boolean;
    protected getQuorum(totalLength: number): number;
}
//# sourceMappingURL=AgreementExecutor.d.ts.map