import { NetworkId } from "../NetworkId";
import { FnBox, FnDelegate } from "./FnBox";
import { FnDelegateConfig } from "./config";
export declare class QuarantinedListFnDelegate implements FnDelegate {
    private curatedList;
    private lastListUpdateTimestamp;
    private healthyIdentifiers;
    private static configCache;
    static getCachedConfig(rpcUrls: string[], networkId: NetworkId): FnDelegateConfig;
    constructor(identifiers: string[], networkId: NetworkId, minimalProvidersCount?: number);
    didFail<R>(fnBox: FnBox<R>): void;
    didSucceed<R>(fnBox: FnBox<R>): void;
    isQuarantined<R>(fnBox: FnBox<R>): boolean;
    private getHealthyIdentifiers;
}
//# sourceMappingURL=QuarantinedListFnDelegate.d.ts.map