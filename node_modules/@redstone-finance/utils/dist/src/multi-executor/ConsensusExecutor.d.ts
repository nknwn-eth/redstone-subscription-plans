import { ParallelExecutor } from "./ParallelExecutor";
export declare abstract class ConsensusExecutor<R> extends ParallelExecutor<R> {
    private readonly quorumRatio;
    constructor(quorumRatio: number, timeoutMs?: number);
    protected verifySettlements<R>(successfulResults: R[], errorResults: unknown[], totalLength: number): boolean;
    private getQuorum;
}
export declare class AllEqualConsensusExecutor<R> extends ConsensusExecutor<R> {
    aggregate(results: R[]): R;
}
export declare class MedianConsensusExecutor<R> extends ConsensusExecutor<R> {
    aggregate(results: R[]): R;
    static getMedian<R>(results: R[]): R;
}
//# sourceMappingURL=ConsensusExecutor.d.ts.map