"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.safelyConvertAnyValueToNumber = void 0;
const logger_1 = require("../logger");
const logger = (0, logger_1.loggerFactory)("utils/numbers");
const safelyConvertAnyValueToNumber = (value) => {
    if (["string", "number"].includes(typeof value)) {
        return Number(value);
    }
    else {
        logger.warn(`Value can not be converted to a valid number. Received: ${String(value)}`);
        return NaN;
    }
};
exports.safelyConvertAnyValueToNumber = safelyConvertAnyValueToNumber;
//# sourceMappingURL=numbers.js.map